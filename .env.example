# Chatbase API key — obtain from chatbase.co dashboard
CHATBASE_API_KEY=

# Airtable personal access token (not legacy API key)
AIRTABLE_API_KEY=

# Airtable base ID — do not change unless migrating to a new base
AIRTABLE_BASE_ID=appy5x5vC5HjN3Ukq

# Shared secret for all internal admin API routes and Airtable webhooks
# Generate with: openssl rand -hex 32
INTERNAL_ADMIN_TOKEN=
